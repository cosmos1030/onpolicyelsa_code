# B200 container handoff — doyoon-pgd9, 2026-09-21/22

Written for whoever picks this up on the next container. This box is torn
down around **10:30 KST on 2026-09-22**. Everything under
`/NHNHOME/log-postech/doyoonkim/` is shared Lustre and survives; the session
transcript does not, which is why this file exists.

## >>> STATE AT TEARDOWN (written 09:40 KST 2026-09-22) <<<

Read this section first; everything below it is history.

**Trainings: all four finished.**

| arm | checkpoint (under /NHNHOME/log-postech/doyoonkim/models/) | on Hub? |
|---|---|---|
| `alps_pgd_tr` (ALPS mask + PGD, klb 0.02) | `gmp_4b_s70pct_lr0.0001_onpol_lmda0.33_20260921_212708_p265824` | yes |
| `alps_pgd_notr` (ALPS mask + PGD, klb 99999) | `gmp_4b_s70pct_lr0.0001_onpol_lmda0.33_20260922_002405_p291199` | yes |
| `cubic_nopgd` (cubic ramp, PGD off) | `gmp_4b_s70pct_lr0.0001_onpol_lmda0.33_20260922_023308_p310907` | yes |
| `frozenpool_4b_s80` (s80, dense rollout pool, never refreshed) | `gmp_4b_s80pct_lr0.0001_onpol_lmda0.33_20260922_062919_p332553` | **NO** -- Hub unreachable since ~06:30 |

Hub repo id = `cosmos1030/` + dir name with `_` -> `-`.

**Seed-42 long evals:**

| arm | status | run name |
|---|---|---|
| `alps_pgd_tr` | log_cluster job 51045 | `s3_4b_alpspgdtr_s70_seed42` |
| `cubic_nopgd` | **DONE 5/5, avg5 49.09** (wandb ay0dnnto) | `s3_4b_cubicnopgd_s70_seed42` |
| `alps_pgd_notr` | cut at teardown: math500/gpqa/ifeval cached, LCB ~50% (not cached), gsm8k not started | `s3_4b_alpspgdnotr_s70_seed42` |
| `frozenpool_4b_s80` | cut at teardown: math500/gpqa/ifeval cached, LCB ~1%, gsm8k not started | `s3_4b_frozenpool_s80_seed42` |

The cached generations sit in hex-named dirs inside each checkpoint dir
(`9abf7d9a67dc6662/` for notr, `a693ed190b4e4638/` for frozenpool). Re-running
the SAME command on a box that sees these dirs reloads those three benchmarks
in seconds and generates only LCB + gsm8k (~3.5 h for 4B). Command:

    bash b200_scripts/eval_long_dedup.sh reasoning_qwen3_4b_nostrip8192 \
        s3_4b_alpspgdnotr_s70 42 <checkpoint dir> gmp 0.7
    bash b200_scripts/eval_long_dedup.sh reasoning_qwen3_4b_nostrip8192 \
        s3_4b_frozenpool_s80 42 <checkpoint dir> gmp 0.8

A Hub id works as <checkpoint dir> too, but then the cache is NOT reused
(it lives in the local dir) and all five benchmarks run again.

**Two open items for the next box:**
1. Upload the frozenpool checkpoint to the Hub from a machine that mounts
   /NHNHOME -- until then log_cluster cannot evaluate it.
2. Finish the two cut evals above, then seeds 0/1 where the paper needs them.
   Most urgent: cubic_nopgd seeds 0/1 -- its single seed ties SCOUT
   (49.09 vs 48.81 +- 0.41) and one seed cannot say which way it leans.

Live status is appended every 10 min to `status_timeline.log` (same dir) by
`status_logger.sh`. Read the tail of that first — it is ground truth for
where things actually got to.

## What this box is producing

Four 4B trainings. Three are the s70 ladder that separates *mask source* from
*PGD* from *trust region*; the fourth tests whether rollout freshness matters
at high sparsity.

| arm | what differs | wandb (training) | status as of 09:40 |
|---|---|---|---|
| `alps_pgd_tr` | ALPS mask fixed + PGD on + trust region on (klb 0.02) | `xybjsc3c` | **done 21:27**, pushed |
| `alps_pgd_notr` | same, trust region off (klb 99999) | `z4uyap8g` | **done 00:24**, eval killed, pushed 00:30 |
| `cubic_nopgd` | no PGD; cubic ramp 0→0.7 then frozen mask | `ccddldfw` | **done**; long eval **complete 06:30**, avg5 49.09 |
| `frozenpool_4b_s80` | s80 SCOUT config, rollout pool filled once from DENSE and never refreshed | (new) | **done 06:29**; eval running, will be cut |

Launchers: `b200_scripts/{alps_pgd,gmp_cubic_noPGD,gmp_frozenpool}_qwen3_4b.sh`.

## Checkpoints

Local dirs under `/NHNHOME/log-postech/doyoonkim/models/`. `push_ckpts.sh`
uploads each one to the Hub as soon as its "Saved pruned model to" line
appears, retrying every 2 min (the Hub was unreachable 21:49–22:02; it
recovered on its own). Repo id = dir name with `_`→`-`.

- `alps_pgd_tr` → `gmp_4b_s70pct_lr0.0001_onpol_lmda0.33_20260921_212708_p265824`
  → hub `cosmos1030/gmp-4b-s70pct-lr0.0001-onpol-lmda0.33-20260921-212708-p265824`
  Milestones at step 512/1024/1536 sit beside it (`..._step000512_...` etc).
- `alps_pgd_notr` → `gmp_4b_s70pct_lr0.0001_onpol_lmda0.33_20260922_002405_p291199`
  (same stem as alps_pgd_tr -- both kd 0.33, s70 -- only the timestamp/pid
  differ; the klb=99999 one is the 00:24 dir).
- cubic and frozenpool land in the same directory with the same naming.
- frozenpool confirmed in the log: `frozen-pool mode (onpolicy_interval=4096
  >= steps=2048) -- initial pool sized to 16384 rollouts`, then
  `initial pool filled with 16384 rollouts`, `Step 1/2048 | sparsity=0.000`
  (pool generated while dense), and 0 `pool refilled` lines as of step 56.

Verified: sparsity 0.7005, Qwen3-4B (hidden 2560, 36 layers), bf16.

## Evals — what is and is not covered

A 4B s70 long eval measures **5.7 h** here (LCB alone 3.2 h), so most evals do
not fit before teardown. Deliberate split:

- `cubic_nopgd` — checkpoint `gmp_4b_s70pct_lr0.0001_onpol_lmda0.33_20260922_023308_p310907`.
  Its built-in eval was **killed at 02:39**: this launcher runs PPL (wikitext)
  and zero-shot BEFORE the long bench, the Hub dropped out again at 02:33, and
  it sat in dataset-download retries. Neither is used by the table, and the
  5.7 h long bench had to start by ~04:45 to finish before teardown. Replaced
  by `eval_long_dedup.sh` with HF_DATASETS_OFFLINE=1 HF_HUB_OFFLINE=1, long
  bench only, run name `s3_4b_cubicnopgd_s70_seed42` (started 02:39, gpu0).
  Harvest label `cubicnopgd` -> "Cubic schedule, no PGD (pace-matched)" --
  deliberately NOT `cubic_matched`, which is the A2sched control that keeps
  PGD on; merging them would hide the difference this arm measures.
- `frozenpool_4b_s80` — built-in eval starts 06:35, **will be cut off** at
  teardown. Not wasted: lighteval caches finished benchmarks' generations
  under the model dir, so a later run resumes from there.
- `alps_pgd_tr` — eval was killed here to free the GPU. **The log_cluster side
  is running it** (slurm job 51045, `s3_4b_alpspgdtr_s70_seed42`). Do not
  duplicate it.
- `alps_pgd_notr` — eval killed at 00:35 to start frozenpool. Nothing has run
  it yet. Best candidate for the next box.

Seeds 0 and 1 were never attempted for any of these; there was no room.

## RESULT (06:30): cubic, no PGD, 4B s70, seed 42 -- avg5 49.09

wandb `ay0dnnto` (`s3_4b_cubicnopgd_s70_seed42`), all five at the long cap:
math500 79.40 / gpqa 28.79 / ifeval 41.77 / lcb 8.58 / gsm8k 86.88.

Against SCOUT s70 (3-seed mean 48.81 +- 0.41) that is +0.28 -- inside one
std. A2sched (cubic WITH PGD) is 48.84. ALPS+retrain is 44.94. So at s70 a
scheduled 0->target fill with no KL-gated PGD at all matches SCOUT, and the
+4 over ALPS+retrain comes from growing the mask, not from PGD's swaps.
Caveats: one seed vs a 3-seed mean; s70 only (s80 is where SCOUT beats
ALPS+retrain by +11.3, and no no-PGD cubic exists there yet). Seeds 0/1 of
this arm, and an s80 version of it, are what would settle it.

**Hub upload of frozenpool may not have happened.** The Hub has been
unreachable since ~06:30 (TCP to huggingface.co:443 fails; wandb and github
fine), and push_ckpts.sh has failed every retry since. It keeps retrying every
2 min until teardown. Check `.pushed_gpu1` for the `..._062919_p332553` line:
if it is absent, the checkpoint exists ONLY on this Lustre path --
`/NHNHOME/log-postech/doyoonkim/models/gmp_4b_s80pct_lr0.0001_onpol_lmda0.33_20260922_062919_p332553`
-- and must be uploaded from a machine that mounts /NHNHOME before log_cluster
can evaluate it. The other three checkpoints did reach the Hub.

Evals running at teardown (both started 06:30, both cut off ~10:30):
gpu0 `s3_4b_alpspgdnotr_s70_seed42`, gpu1 `s3_4b_frozenpool_s80_seed42`.
Frozenpool checkpoint: `gmp_4b_s80pct_lr0.0001_onpol_lmda0.33_20260922_062919_p332553`.

## Automation armed for the last hours (03:43)

- `frozen_handoff.sh` waits for frozenpool's "Saved pruned model to" line,
  PREPENDS an offline long-bench eval of it to `4bqueue.txt`
  (`s3_4b_frozenpool_s80_seed42`), then kills the launcher's own eval phase
  (PPL/zero-shot would stall on the Hub the way cubic's did). gpu1 pops it.
- `4bqueue.txt` already holds `notr_eval` (`s3_4b_alpspgdnotr_s70_seed42`,
  offline, dedup-checked) for gpu0 once cubic's eval ends ~08:25.
- Neither eval finishes before ~10:30 teardown. Finished benchmarks' generations
  are cached under each model dir, so the next box only runs what is left --
  run them through eval_long_dedup.sh so a covered cell is skipped.

## Gotchas found the hard way today

1. **Never edit a running bash script in place.** bash reads a script by byte
   offset as it executes. Editing `eval_8b_long.sh` on 2026-09-20 shifted
   every later offset under two running jobs; both resumed mid-word inside
   `--skip_zeroshot`, printed `line 123: o-shot: command not found`, and re-ran
   their whole benchmark suite — 5.6 GPU-hours and two duplicate wandb runs.
   Write a new file and `mv` it over: rename keeps the running process on the
   old inode.
2. **`eval_full.py` exits 0 even when every benchmark died.** An
   `EngineDeadError` run and an OOM run both reported rc=0 with no metrics.
   `run_4b_curve.sh` now counts `results_*.json` and logs `INCOMPLETE` when
   fewer than 5 landed. Check for that string before trusting a DONE.
3. **Free-GPU detection must look at processes, not memory.** eval_full.py
   drops its vLLM engine between benchmarks, so a busy card reads near-empty
   for minutes. A memory-only check started a second job on gpu0 on 09-20 and
   cost `alps_s70` seed0 its LCB and GSM8K to OOM. `gpu_busy()` in
   `seed_worker.sh` / `run_4b_curve.sh` now also matches any `eval_full.py` /
   `resume_eval_lighteval.py` whose `CUDA_VISIBLE_DEVICES` is that card.
4. **`pop()` left the last queue line in place.** `grep -v ... && mv` skips the
   mv when grep filters out the only line, so the final task ran twice. Fixed
   to an unconditional mv.
5. **The 4B launchers defaulted to the 8B wandb project** (fork inheritance).
   Both 4B PGD trainings logged into `reasoning_qwen3_8b_nostrip8192`. Harmless
   this time — harvest admits a run only if it has `<bench>_avg_gen_cap`, and
   their evals were killed — but fixed in commit `b9cfaee`. Pass
   `--wandb_project reasoning_qwen3_4b_nostrip8192` explicitly when evaluating.
6. **`flock` does not work on this Lustre mount.** Two workers started at the
   same instant popped the same task. Start queue workers staggered, and
   confirm the queue shrank before starting the second.

## Before starting ANY eval: check wandb for a duplicate

`b200_scripts/wandb_dup_check.py <project> <run_name> <seed> [model_path]`
exits 0 if that eval is already covered, 1 if not, 2 if wandb was
unreachable. Covered means the same TSV cell (harvest's own label + sparsity
+ seed, parsed out of harvest_long_tsv.py so the two cannot drift) or the same
checkpoint (exact `config.model_path` match over its local/Hub spellings),
and either finished at 5/5 long-profile benchmarks or running with a
heartbeat under 30 min. A run that lost benchmarks to OOM does NOT count.

Wired in already: `eval_8b_long.sh` drops covered seeds and exits 0 if none
remain; `eval_long_dedup.sh` is the same guard around a bare eval_full.py
call. Verified on known cases (2-22 s each): 4B s70 alpsretrain 3term s42 ->
dup (n84 running), 2term s1 -> dup (n86), alpspgdnotr s42 -> none, 8B s80
alps s1 -> dup (bdqripdh finished 5/5), and a deliberately misnamed run on the
2term checkpoint -> dup via model_path.

Why: on 2026-09-21 this box spent 4.8 GPU-hours on a 4B eval that n84 was
already running, and none of the three machines can see each other's queues.
Known gap: an old-style Hub id (gmp-kd3e-1-...) does not map back to a local
dir name, so path matching misses it -- the label match still catches it if
the run is named s3_<size>_<arm>_s<NN>_seed<N>.

## Naming so results land in the right TSV block

`harvest_long_tsv.py` derives a block label from the run name: it strips
`s3_<size>b_`, `_seedN`, and the `_sNN` sparsity token, then looks the rest up
in `METHOD`. Labels already registered for these arms (commit `74a09a2`):
`alpspgdtr` → "ALPS + PGD (trust region)", `alpspgdnotr` → "ALPS + PGD (no
trust region)". So name eval runs `s3_4b_alpspgdtr_s70_seed42` and they merge
correctly. Get this wrong and the arm opens a second one-seed block.

## Daemons running here (all die with the container)

| script | job |
|---|---|
| `run_4b_curve.sh {0,1}` | one slot per GPU; pops `4bqueue.txt`, then hands the GPU back to `seed_worker.sh` |
| `push_ckpts.sh {0,1}` | uploads every new checkpoint to the Hub, retries on failure |
| `skip_eval_gpu1.sh` | kills `alps_pgd_notr`'s built-in eval so frozenpool can start |
| `status_logger.sh` | this file's companion timeline |

Nothing needs restarting on the next box unless you want to continue the
queue mechanism; the queues themselves (`4bqueue.txt`, `seedqueue.txt`) are
plain text in this directory.

## What the next box should probably do

1. Read `status_timeline.log` tail, confirm which of the four trainings
   finished and which checkpoints reached the Hub (`.pushed_gpu0`, `.pushed_gpu1`).
2. Run the missing seed-42 evals: `alps_pgd_notr`, and finish `frozenpool_4b_s80`
   (its cache means only the unfinished benchmarks re-run).
3. Then seeds 0 and 1 for whichever arms the paper needs error bars on.
